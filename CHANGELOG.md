# 更新记录

## V2.3.1：自动更新仓库地址热修复

- 将自动更新默认地址改为正式仓库 `3322810958-sudo/YXRT_Money_APP`，修复仓库改名后下载地址被安全校验拒绝的问题。
- 保持 V2.3.0 的全部功能和数据结构不变，可从任意旧版本直接无损升级。

## V2.3.0：质量追溯、证明附件、个人界面与兼容性优化

- 离线 OCR 增加字段级置信度；低置信度、导入或识别失败、PDF 导出或转换失败分别采用黄色、红色、紫色标记，并统一进入右下角故障追溯框。
- 批量 ZIP 或多文件导入改为逐张容错，一张失败不会终止整批；完成后显示逐张校对卡片，可进入与单张发票完全一致的编辑界面检查分类、资金来源、金额和日期。
- 发票新增购物证明、签字证明和其他证明附件，支持图片、PDF、Office、文字、视频及压缩包等格式。
- 新增“发票 + 证明附件”导出：可转换内容作为独立 PDF 页面追加，避免遮挡；不能转换的源文件保留在 ZIP 中并写入清单。
- 发票源文件改为调用 Windows 默认程序直接打开，不再依赖在线网页或第三方登录。
- 登录页增加面板透明度、高对比输入、背景随机；忘记密码可使用原始管理员凭据本机验证重置，并具备失败限速、审计和管理员启停/更换设置。
- 增加成员头像、个人音频导入和顺序/单曲/随机播放；声音默认关闭，不提供或引用来源不明音乐。
- 增加“用户名软件设置.zip”导入导出，可选择包含个人媒体；不包含密码、会话、发票和其他业务数据。
- 增加问题反馈模块：本机打包多格式附件，优先发往指定 QQ 邮箱；失败后保留待发送队列，GitHub 备用提交自动隐藏姓名、联系方式和附件。
- 增加当前表单保存、撤销、恢复悬浮工具条；历史只存在当前未保存草稿，不回退已写入数据库的数据。
- 创作者新增“2026赛季 / 高压 / 段力裴”，创作者页列出许可证明确的开源参考链接，并注明仅供燕翔车队内部非商业发票报销。
- 取消启动时默认预热大型 OCR 模型，首次显示界面更快；需要时再加载识别引擎，改善低配置 Windows 与完整文件夹转发场景。
- 图片和音乐只允许使用用户自有、获授权或许可证明确素材；未使用来源不明媒体。

## V2.2.4：人员与组别无损升级、完整备份

- Windows 软件首次启动 V2.2.4 时，将既有数据库、附件、模型和备份迁移到固定数据区；以后更换程序文件夹也继续使用同一份业务数据。
- 修复启动迁移反复补入默认组别的问题，升级与恢复不会重置现有人员、组别或账号关联。
- 完整备份新增人员、组别、账号关联和发票明细可查看清单；数据库仍完整保存全部赛季、设置、日志、回溯与附件索引。
- 恢复备份会校验数据库完整性、人员账号关联和清单数量，恢复后成员资料、组别和登录账号保持原样。
- 版本更新新增“自动完整备份并更新”和“手动下载补丁包并覆盖”两种方式；自动安装前备份失败会立即停止更新。
- 本版本只提供轻量补丁 ZIP，不包含或覆盖任何业务数据目录，也不重新生成教程 PDF。

## V2.2.3：创作者全赛季共享

- 创作者名单改为全赛季通用，历史赛季和以后新建赛季均显示同一份名单。
- 管理员在任意赛季新增、修改、隐藏或删除创作者，其他赛季同步生效。
- 现有创作者记录自动保留，不需要重新添加，也不会被单个赛季的版本回溯覆盖。
- 本版本仅提供轻量无损补丁，不重复打包运行库、OCR 模型或教程 PDF。

## V2.2.2：附件预览、原画质 PDF 与轻量更新

- 单张录入与批量导入均显示附件缩略图，点击后可在居中窗口放大、缩小或打开原文件。
- 新增独立“批量导出 PDF”按钮：可合并为一个 PDF，也可每张发票生成一个 PDF 后打包 ZIP。
- 源文件为 PDF 时直接使用页面内容，图片按原始像素无缩放、无损封装，避免画质下降。
- CSV 固定为发票号码、总金额、分类、承担方式、资金来源、成员六列。
- 管理员可设置录入和批量导入的默认分类、垫付成员、资金来源、承担方式、指定成员、批次备注及承担方式名称。
- 增加石墨灰、午夜紫、工程青、暖灰护眼等显示模式和五档背景清晰度预设，壁纸显示更明显。
- 修复 Windows 软件“下载备份”无反应，改为主动选择保存位置并显示进度、文件大小和成功提示。
- 所有成员与公共查看账号均可检查、下载和安装更新。
- 自动更新补丁改为仅打包主程序及网页资源；数据库、附件、账号、壁纸、模型和设置继续原样保留。

## V2.2.1：赛季隔离与创作者名单

- 增加管理员专属赛季管理，可新建、切换、改名、归档和重新启用赛季。
- 现有成员、成员账号、发票、分摊、结算、日志和版本自动归入 2026 赛季，旧数据不丢失。
- 新赛季不沿用原成员和成员账号，需要重新建立；电气部、底盘部、车身部、市场部等长期组别继续保留。
- 历史赛季可随时切换查看；归档赛季为只读状态，只有管理员可以切换赛季。
- 管理员账号和公共查看账号跨赛季保留；成员账号只允许在所属赛季登录。
- 增加所有人可查看、仅管理员可编辑的创作者页面，预置“2026赛季 / 电气部 / 高压 / 刘松宁”。
- 自动更新继续保留数据库、附件、账号、外观媒体、设置和 OCR 模型。

## V2.2.0：批量台账、赛车进度与无损更新

- 修复 Windows 桌面软件点击 CSV 导出无响应，导出结束后显示发票数量和合计金额。
- 增加发票多选、全选、批量分类、批量报销状态、批量删除和选中项导出。
- 发票台账及 CSV 同时显示开票日期、上传日期、提交人及提交账号。
- 支持 ZIP 或多个图片、PDF、TXT、OFD 文件直接批量导入，并汇总成功数量与识别金额。
- 批量导入、离线识别和更新下载使用随机赛车 0～100% 进度动画；管理员可维护赛车图片。
- OCR 根据 CPU 自动选择工作线程并后台预热，对超大图片限边处理，重复附件复用识别结果。
- 增加 GitHub Release 自动检查、下载、SHA-256 校验、安装重启和更新日志。
- 自动更新与旧版本桥接补丁均保留数据库、附件、账号、设置、壁纸和 OCR 模型；替换失败自动恢复旧程序。
- 修正 GitHub 自动测试与 Windows 发布流程的依赖缓存路径，保证后续提交和版本构建可持续验证。

## V2.1.1：高可读性与快捷操作

- 增加清晰深色、护眼浅色和赛车蓝高对比三种显示模式，每台电脑独立记忆选择。
- 提高弹窗、表格、分类规则和辅助文字的字号、背景不透明度与颜色对比度。
- 删除、停用、回溯、恢复等操作统一改为软件内居中确认框，取消浏览器原生白色提示框。
- 增加可自定义快捷键、重复冲突检测、清除单项及恢复默认功能。
- 图表颜色随显示模式自动适配，浅色模式下仍保持清晰可读。

## V2.1：智能分类与动态外观

- 接入 Wallpaper Engine 本地创意工坊库，可预览并导入图片、GIF、视频；场景类和网页类壁纸使用预览图。
- 登录页支持管理员维护的图片、GIF、视频轮播，可调整顺序、标题、时长和切换效果。
- 登录页增加密码显示与隐藏，主要操作文字统一为简体中文。
- 离线 OCR 后自动推断费用分类和物品类型，内置继电器、紧固件、耗材、电器件等车队采购规则。
- 管理员可增删改分类关键词、优先级和启用状态；人工修正后的历史记录会在本机参与后续判断。
- 增加公开登录外观接口、媒体安全校验、分类规则接口及对应自动化测试。
- 提供 GitHub 开源结构、Windows 自动测试工作流、贡献规范、安全说明和脱敏规则。

# V2.0 三轮迭代记录

## 第一轮：核心账务与权限

- 建立 SQLite 长期数据库、成员、账号、发票、资金来源和分类结构。
- 增加管理员、成员、公共查看三种角色与独立登录。
- 完成发票增删改查、演示数据、金额精确到分及基础驾驶舱。

## 第二轮：识别、分摊与统计

- 集成 PP-OCRv5 中文轻量模型，支持图片、PDF 和 ZIP 批量导入。
- 增加全队、指定成员、等额、权重、自付等分摊方式。
- 增加报销进度、成员往来、还款记录、分类与资金来源统计。
- 增加费用分类与资金来源的可视化新增、编辑、停用和重新启用功能。
- 增加自定义背景、队标和自适应赛车驾驶舱界面。

## 第三轮：同步、回溯与交付

- 增加离线队列、附件校验和云端双向同步，冲突按更新时间与设备标识处理。
- 增加审计日志、自动保护点、手动版本和管理员回溯。
- 增加完整备份与恢复、CSV 导出、Docker 云端部署文件。
- 完成 Windows 免安装封装、测试和 PDF 使用教程。
